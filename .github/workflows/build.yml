name: Build APK

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Клонирование репозитория
        uses: actions/checkout@v3

      - name: 🐍 Установка Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: ⚙️ Установка зависимостей
        run: |
          python -m pip install --upgrade pip
          pip install buildozer Cython

      - name: 📦 Установка Java и Buildozer зависимости
        run: |
          sudo apt update
          sudo apt install -y zip unzip openjdk-17-jdk libsdl2-dev \
            libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev \
            libportmidi-dev libswscale-dev libavformat-dev libavcodec-dev \
            zlib1g-dev libgstreamer1.0 libmtdev-dev \
            libgl1-mesa-dev libgles2-mesa-dev \
            libgstreamer-plugins-base1.0-dev \
            libpulse-dev libffi-dev

      - name: 🏗️ Сборка APK
        run: |
          buildozer android debug

      - name: 📤 Загрузка APK как артефакт
        uses: actions/upload-artifact@v3
        with:
          name: sklad-apk
          path: bin/*.apk
